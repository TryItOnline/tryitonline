#!/usr/bin/env bash

range=$1
wrapper_exec=/srv/bin/wrapper
wrapper_temp=/tmp/wrapper.$range
mkdir $wrapper_temp
chcon -t sandbox_file_t -l $range $wrapper_temp
timeout 65s sandbox -l $range -H $wrapper_temp -T $wrapper_temp $wrapper_exec
killall -Z $range -KILL
rm -r $wrapper_temp
