#!/usr/bin/env bash

range=$1
sandbox_exec=/srv/bin/run-sandbox
sandbox_temp=/tmp/sandbox.$range
mkdir $sandbox_temp
chcon -t sandbox_file_t -l $range $sandbox_temp
timeout -s KILL 65s sandbox -l $range -H $sandbox_temp -T $sandbox_temp $sandbox_exec
killall -Z $range -KILL
rm -r $sandbox_temp
