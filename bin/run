#!/usr/bin/env bash

range=$1
comm=$2
temp=/tmp/sandbox.$range
mkdir $temp
chcon -t sandbox_file_t -l $range $temp
timeout -s KILL 65s sandbox -l $range -H $temp -T $temp $comm
killall -Z $range -KILL
rm -r $temp
